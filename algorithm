1. Josepy game:
def Jos(a):
    while len(a) > 3:
        b = []
        for i in range(len(a)):
            if (i+1)%3 != 0:
                b.append(a[i])
            else:
                print(a[i])
        a = b

def Jos2(a):
    if len(a) < 3:
        return

    b = []
    for i in range(len(a)):
        if (i+1)%3 != 0:
            b.append(a[i])
        else:
            print(a[i])
    Jos2(b)

a = [i for i in range(1,1600000)]
Jos2(a)

2. 
